name: Patch header logo

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message != 'Use transparent header logo'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch logo reference
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          old = '<img src="./Logo%20LED-Trailer.png?v=2" alt="LED-Trailer.com" width="184" height="51" style="display:block;height:auto;max-height:51px">'
          new = '<img src="./led-trailer-logo-header.svg?v=3" alt="LED-Trailer.com" width="210" height="49" style="display:block;height:auto;max-height:49px">'
          if old not in s:
              raise SystemExit(0)
          p.write_text(s.replace(old, new, 1), encoding='utf-8')
          PY
      - name: Commit change
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Use transparent header logo"
          git push
